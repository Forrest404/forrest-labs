import ComingSoon from '../_coming-soon'

// Reports — CLAUDE.md build-area 5 (one-tap OCHA-style sitrep, broadcasts,
// inter-agency board, exports). Scaffolded so it appears in the persistent nav;
// the feature itself is built later.
export default function NgoReportsPage() {
  return (
    <ComingSoon
      title="Reports"
      blurb="One-tap sitreps drafted from incidents and dispatches, broadcasts to field staff, and GeoJSON/CSV export will live here."
    />
  )
}
