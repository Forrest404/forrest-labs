import ComingSoon from '../_coming-soon'

// Facilities & contacts — directory scaffold (hospitals, shelters, key contacts).
// Appears in the persistent nav; the feature itself is built later.
export default function NgoFacilitiesPage() {
  return (
    <ComingSoon
      title="Facilities & contacts"
      blurb="A directory of hospitals, shelters, safe routes and key contacts for the operational area will live here."
    />
  )
}
