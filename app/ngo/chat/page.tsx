import ComingSoon from '../_coming-soon'

// Group chats — coordination channel scaffold. Appears in the persistent nav so
// the menu reflects the full vision; the feature itself is built later.
export default function NgoChatPage() {
  return (
    <ComingSoon
      title="Group chats"
      blurb="Structured, low-bandwidth team coordination — distinct from the panic and dispatch channels — will live here."
    />
  )
}
