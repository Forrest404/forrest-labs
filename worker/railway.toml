[build]
builder = "DOCKERFILE"
dockerfilePath = "worker/Dockerfile"

[deploy]
startCommand = "gunicorn --bind 0.0.0.0:8000 --workers 2 --timeout 180 main:app"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
